<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inicio</title>
</head>
<body>
  <h1>Productos</h1>
  {{#each products}}
    <div>
      <h2>{{title}}</h2>
      <p>{{description}}</p>
      <p>Precio: ${{price}}</p>
      <p>Stock: {{stock}}</p>
      <img src="{{thumbnail}}" alt="{{title}}">
    </div>
  {{/each}}
</body>
</html>
